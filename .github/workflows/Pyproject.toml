[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "amienigma-ai"
version = "0.0.1"
description = "Amienigma Universe AI archive project"
authors = [
  { name="Amienigma" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "requests"
]
